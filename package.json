{
  "name": "golf-pool",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "The Open Championship 2026 golf pool tracker — static site with client-side scoring & live ESPN sync",
  "scripts": {
    "generate-data": "node scripts/generate-data.js",
    "dev": "npm run generate-data && npm --prefix web run dev",
    "build": "npm run generate-data && npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "postinstall": "npm --prefix web install"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
