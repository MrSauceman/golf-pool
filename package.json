{
  "name": "golf-pool",
  "version": "1.0.0",
  "private": true,
  "description": "Open Championship 2026 golf pool tracker - live leaderboard & team drill-down",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix web install",
    "import": "npm --prefix server run import",
    "dev": "concurrently -n api,web -c green,cyan \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
